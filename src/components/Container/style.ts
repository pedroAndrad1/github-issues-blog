import { styled } from 'styled-components'

export const StyledContaier = styled.main`
  max-width: 864px;
  margin: 0 auto;
  margin-top: -5rem;

  @media (max-width: 900px) {
    padding: 0 10vw;
  }
`
