export interface Issue {
  number: number
  body: string
  createdAgo: number
  title: string
}

export interface UserData {
  avatarUrl: string
  bio: string
  company: string
  followers: string
  htmlUrl: string
  login: string
  name: string
}
